# ============================================
# Risk Radar - Production Environment Template
# ============================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root on your VPS
# 2. Fill in all placeholder values marked with <...>
# 3. Generate secure random values for secrets
#
# To generate random secrets, use:
#   openssl rand -base64 64
#
# ============================================

# === DOMAIN CONFIGURATION ===
HOSTNAME=riskradar.ovh

# === JWT SECRETS (REQUIRED) ===
# Generate with: openssl rand -base64 64
JWT_ACCESS_SECRET=<GENERATE_64_CHAR_RANDOM_STRING>
JWT_REFRESH_SECRET=<GENERATE_64_CHAR_RANDOM_STRING>

# === DATABASE (REQUIRED) ===
# Generate password with: openssl rand -base64 32
POSTGRES_USER=riskradar_prod
POSTGRES_PASSWORD=<GENERATE_SECURE_PASSWORD>

# === SUPERADMIN ACCOUNT ===
# This password will be used for the superadmin@riskradar.ovh account
# Generate with: openssl rand -base64 24
SUPERADMIN_PASSWORD=<GENERATE_SECURE_PASSWORD>

# === GOOGLE AI (OPTIONAL) ===
# Required for AI Assistant Service (threat analysis)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=<YOUR_GOOGLE_AI_API_KEY>
GOOGLE_AI_MODEL=models/gemini-2.5-flash

# === CLOUDFLARE TUNNEL (OPTIONAL) ===
# Only needed if running cloudflared in Docker
# Get from Cloudflare Zero Trust Dashboard > Access > Tunnels
CLOUDFLARE_TUNNEL_TOKEN=<YOUR_TUNNEL_TOKEN>

# === OPTIONAL OVERRIDES ===
# Uncomment and modify if needed

# Log retention (default: 365 days)
# LOG_RETENTION_DAYS=365

# Demo mode (default: false) - blocks DELETE operations
# DEMO_MODE=false
